# Session Log

## 2026-04-08

### Morning Session - Planning Phase
- Completed: Reviewed the greenfield repo state, identified the relevant kickoff skills and plugins, shaped the product direction toward a lean MVP SaaS for a used-vehicle reseller, researched current dealer inventory and listing patterns plus modern production web-app UX patterns, wrote the product spec to `docs/superpowers/specs/2026-04-08-vehicle-inventory-portal-design.md`, wrote the screen architecture to `docs/superpowers/specs/2026-04-08-vehicle-inventory-portal-screen-architecture.md`, wrote the implementation plan to `docs/superpowers/plans/2026-04-08-vehicle-inventory-portal-implementation-plan.md`, created `HANDOFF.md`, and updated the planning docs to include in-app chat with both text messages and voice notes handled by Admin and Manager.
- In progress: No code implementation had started yet. The project was ready to move from design and planning into execution.
- Remaining or blocked: No product blockers remained. Implementation could start from Chunk 1 of the implementation plan. Open execution details remained around the exact plate-blur provider credentials, final visual design tokens, notification delivery method, and browser-specific recording edge cases for voice notes, but none blocked scaffolding or core architecture work.
- Key decisions made and why: Treated the workspace as a greenfield start because the repo root was effectively empty, so kickoff and architecture skills were more useful than maintenance workflows. Recommended a private inventory portal instead of a public marketplace because the product is single-tenant, hides broker mechanics from end users, and needs approval-gated access with restricted financial visibility. Chose Supabase for database, auth, and storage because it fits the MVP cleanly and can handle a large private image library with blurred public variants. Added bilingual English and Hindi support plus strong UI and UX quality as first-class product requirements because the whole app, including staff workflows, must be easy to operate and locally usable. Added modern web-app patterns such as global search, saved views, quick preview drawers, queue-driven staff UX, and keyboard-friendly workflows. Chose auto-detect on first visit with language switching on onboarding and auth pages and later from profile or settings so the app stays simple during normal use while preserving preference. Added both text chat and voice notes to the MVP because the user base may include buyers and sellers with lower comfort using text-only interfaces.
- Known issues or tech debt introduced: None.

### Afternoon Session - Implementation Phase
- Completed:
  - Created the Next.js project with TypeScript, App Router, and `src/`
  - Installed core dependencies including `next-intl`, Supabase clients, Zod, React Hook Form, Lucide, and `idb`
  - Installed PWA dependencies including Serwist packages
  - Installed dev dependencies including Vitest, Testing Library, Playwright, ESLint, Prettier, Husky, and lint-staged
  - Configured Tailwind CSS `3.4.17` and shadcn/ui with 23 components
  - Set up i18n with English and Hindi translations
  - Created the PWA manifest and service worker with Serwist
  - Implemented offline support with IndexedDB for vehicles and pending actions
  - Created `useOnlineStatus` and `useOfflineQueue`
  - Created the offline indicator component
  - Created locale-based routing with a `[locale]` route group
  - Created auth pages for login and signup
  - Created initial auth API route handlers
  - Updated the implementation plan with offline, visual testing, and long-term maintenance additions
- In progress: Building the app shell and navigation in Chunk 1 Task 3.
- Remaining or blocked: Needed to configure Supabase fully, create database schema and migrations, and build the app layout with sidebar and topbar.
- Key decisions made and why:
  - Used Serwist instead of `next-pwa` for better Next.js 16 and Turbopack support
  - Used `useSyncExternalStore` for online status to avoid React 19 warnings
  - Simplified Link usage in forms to avoid complex shadcn Link integration issues
  - Added the required shadcn/ui components early to avoid repeated setup later
- Known issues or tech debt introduced:
  - API routes were still stubs and needed real Supabase integration
  - Database migrations still needed to be created
  - Service worker behavior still needed production validation

### Pre-Implementation Plan Updates
- Added PWA support with a Serwist service worker
- Added IndexedDB for offline data caching and a sync queue
- Added visual regression testing with Playwright
- Added code quality tools including ESLint, Prettier, Husky, and lint-staged
- Added global error and not-found boundaries
- Added optional Sentry environment variables

### Implementation Plan Status
- Chunk 1 Task 1: Completed. Bootstrap Next.js workspace.
- Chunk 1 Task 1B: Completed. PWA and offline support integrated into Task 1.
- Chunk 1 Task 2: In progress. Set up locale-aware routing and dictionaries.
- Chunk 1 Task 3: In progress. Build app shell and shared navigation.

### Evening Session - Audit and Hardening
- Completed: Ran a repo-wide audit focused on runtime safety, routing, auth and demo mode, offline sync, RLS and storage security, mobile navigation, accessibility, and lint and type health. Fixed locale-aware navigation to use the `next-intl` routing helpers, removed dead sidebar and dashboard links, added a mobile sidebar trigger, persisted the demo login session via cookie, unified server auth reads through `getAuthenticatedUser()`, fixed static asset exclusions in `src/proxy.ts`, made offline sync fail safely on non-2xx responses, added the missing Vitest setup file, cleaned all lint warnings, and added a shared placeholder vehicle image so empty-media states stop returning 404s. Reworked vehicle card, detail, and grid components to fix zero-value rendering, broken button types, icon-only accessibility labels, missing mobile affordances, and missing placeholder handling. Reworked the chat provider so it is a client component, computes unread counts from `last_read_at`, marks threads as read correctly, and avoids the previous broken thread and message loading flow. Added `supabase/migrations/20260408_005_security_and_chat_fixes.sql` to add `chat_participants.last_read_at`, lock down broken chat, message, inquiry, and media policies, and make both storage buckets private with participant and admin-only access rules.
- In progress or partially done: The app shell and inventory experience are still mostly placeholder UI backed by static or demo content rather than live Supabase-backed listing queries. Several product workflows exist as reusable components but are not yet wired to API handlers or server actions.
- Remaining or blocked: `npm test` still fails because there are no test files in the repo yet. Chat image messages and playable voice note UI are not fully implemented beyond storage and message plumbing. The current app does not yet resolve private storage paths into signed URLs for media delivery, so the new storage policies are in place but the data-access layer still needs a signed-URL or proxy approach before real private media can be displayed in production. There is also still a root-level stray `nul` file and several greenfield and untracked project files because this repo has not been normalized into a clean committed baseline yet.
- Key decisions made and why: Chose additive fixes over large rewrites so the audit could close the highest-risk issues quickly without destabilizing unrelated scaffolding. Added a new Supabase migration instead of editing existing migrations in place so database environments can apply the hardening incrementally and transparently. Switched the default `dev` script to webpack and suppressed the known Serwist and Turbopack false-positive warning in npm scripts so local verification output reflects real failures.
- Known issues or tech debt introduced: Some large UI files remain over the preferred size limit and still need decomposition once the real data flows are implemented. Private media delivery still requires a follow-up implementation pass. There is still no automated test coverage for auth, navigation, chat, offline sync, or RLS-sensitive flows.

### Late Evening Session - Dashboard Blank Fix
- Completed: Investigated the blank dashboard report against the local server on port `5000`, reproduced the problem, and traced it to invalid nested document markup. `src/app/[locale]/layout.tsx` was rendering its own `<html>` and `<body>` even though `src/app/layout.tsx` already owns the document shell. Removed the nested document wrapper, passed `locale` directly into `NextIntlClientProvider`, and verified the fix by logging into the demo app and confirming `/en/app` returns dashboard content with `Welcome back!`.
- In progress or partially done: The dashboard now renders, but it still uses placeholder KPI data and needs real inventory-backed queries.
- Remaining or blocked: None for the blank-screen bug itself. Browser clients may need a hard refresh if they still hold the broken tree from before the layout fix.
- Key decisions made and why: Fixed the layout at the source instead of adding client-side guards because nested `<html>/<body>` output causes invalid App Router markup and unstable hydration across the whole locale subtree.
- Known issues or tech debt introduced: None.

### Night Session - Documented MVP Route Pass
- Completed: Read the product docs again and aligned the app with the documented screen architecture instead of the earlier placeholder shell. Added a demo-domain data layer under `src/lib/demo/*`, a real viewer and role helper in `src/lib/auth/viewer.ts`, and a local JSON-backed vehicle store at `data/demo-vehicles.json` plus `/api/demo/vehicles` create and update endpoints. Reworked the authenticated app shell to be role-aware, including the sidebar, topbar, locale entry redirects, root redirect, and dashboard split between user and staff experiences. Replaced the placeholder inventory page with a working inventory screen and added the documented `reserved`, `sold`, `vehicle detail`, `inquiries`, `chat`, `my requests`, and improved `profile` routes. Added the full documented staff route tree for `users`, `vehicle management`, `seller submissions`, `reservation requests`, `waitlist`, `resale requests`, `chat inbox`, and `settings`. Added create and edit vehicle pages plus a working local vehicle form wired to the demo vehicle API so staff can now add and edit vehicles in local mode.
- In progress or partially done: Several action flows outside vehicle create and edit are still UI-complete but backed by demo toasts or static queue data rather than real Supabase mutations. Inquiry submission, reservation approval, waitlist promotion, resale approval, and chat persistence still need backend integration beyond the documented screens now being reachable.
- Remaining or blocked: The app still lacks full Supabase-backed live data for every route. The manager and admin operational queues are rendered from demo records, not a production queue layer. Vehicle create and edit persist only to the local demo JSON store and do not yet sync to Supabase tables. Private media delivery still needs signed URL support before real locked-down storage can be shown in production.
- Key decisions made and why: Used the existing documentation as the approved product spec and implemented against it directly instead of reopening discovery. Chose a local demo data layer and a JSON-backed vehicle store as the fastest correct path to make the documented app structure work end to end in local development without waiting on full backend completion. Aligned the permission helper with the docs so manager and admin users can access vehicle management, inquiry queues, reservation queues, and chat inbox flows as originally intended.
- Known issues or tech debt introduced: A few large UI files still need decomposition. The demo domain currently mixes static seed queues with a writable vehicle store, so a later cleanup should consolidate this into one consistent data access layer. The `apply_patch` tool failed repeatedly at the workspace layer during this session, so file updates had to use a PowerShell fallback.

### Late Night Session - Editable Demo Portal, Premium UI, and Media Compression
- Completed: Replaced the earlier mixed static/demo portal behavior with a persisted JSON-backed portal state in `src/lib/demo/portal-store.ts` and related workflow modules for users, requests, seller submissions, settings, chat, and activity history. User profile editing now persists, admin settings now persist, admin user management now supports role, approval, and financial-access edits plus bulk approve, reject, and disable actions, and the staff queues for inquiries, reservation requests, waitlist, resale requests, and seller submissions now perform real demo-backed mutations instead of placeholder behavior. Chat thread creation, staff close-thread flow, and message sending now persist through `/api/demo/portal`, and users can open vehicle-specific chats directly from the detail screen. The dashboard, sidebar, topbar, cards, and inventory surfaces were redesigned toward the documented premium operational UI direction, with role-aware quick actions and calmer inventory presentation. User pricing was hidden from non-staff in inventory and vehicle detail to match the product spec. Added client-side vehicle-image compression to WebP in `src/lib/media/compress-image.ts` and integrated it into the vehicle form through `src/modules/inventory/components/photo-upload-field.tsx`, so uploaded vehicle images are resized and compressed before storage without an obvious visible-quality drop. Locked down `/api/demo/vehicles` and `/api/demo/vehicles/[listingId]` so only staff can create or update listings.
- In progress or partially done: The app now fulfills more of the documented intent in local demo mode, but some workflows are still not fully production-complete. Private media still uses direct data URLs in demo mode rather than signed private URLs, and the documented automatic number-plate blur pipeline is still not implemented. The auth shell still lacks the documented forgot-password flow. Some staff niceties from the spec such as assign-to-me, mark-unread, preview drawers, and broader bulk actions are still not built.
- Remaining or blocked: Full Supabase-backed persistence for inventory, inquiries, reservations, resale, settings, chat, and seller submission workflows remains outstanding. Signed URL delivery for private images and voice notes is still required before the hardened storage policies can be used end to end in production. Plate-blur processing is still pending. Automated test coverage is still absent even though lint, typecheck, and production build now pass. Visual and end-to-end smoke tests remain manual.
- Key decisions made and why: Chose to push the demo mode much closer to the final product intent instead of waiting for all Supabase integrations, because it unlocks meaningful route, role, and action validation right now. Prioritized editable workflows, permissions, and operational clarity over adding more static pages. Used client-side compression for uploaded vehicle images as the fastest correct way to reduce storage pressure immediately while keeping the code local and dependency-free. Rewrote one malformed dashboard file through PowerShell because it had invalid UTF-8 bytes and could not be updated through `apply_patch`.
- Known issues or tech debt introduced: The persisted demo store now spans multiple JSON-backed domains and should eventually be replaced by a single production data layer. Some components, especially dashboard and queue-related surfaces, remain larger than the preferred size and should be decomposed later. The local HTTP smoke test created a runtime test inquiry in demo data during verification. PowerShell file reads on Windows can display Hindi text incorrectly unless `-Encoding utf8` is specified, so localization file inspection needs that flag during debugging.

### 2026-04-09 Early Session - Review Pass and Core Spec Gap Fixes
- Completed: Re-ran the codebase against the product spec and screen architecture, then fixed a new group of core gaps. Added a real forgot-password route at `src/app/[locale]/(auth)/forgot-password/page.tsx` plus the supporting client form in `src/modules/auth/components/forgot-password-form.tsx` and the `reset-password` branch in `src/app/api/auth/[...path]/route.ts`. Added a reusable auth-shell language switcher in `src/components/layout/language-switcher.tsx` and integrated it into the auth layout, pending approval screen, and access denied screen. Improved root-locale behavior by adding `src/lib/i18n/preferred-locale.ts` and updating `src/app/page.tsx` so `/` now redirects using locale preference and `Accept-Language` instead of always forcing `/en`. Fixed a real permissions bug in `src/lib/supabase/permissions.ts` where managers were always treated as having financial visibility even when admin settings disabled it. Added `src/app/[locale]/error.tsx` and `src/app/not-found.tsx` for safer App Router fallbacks. Upgraded the top bar quick-actions surface with a `Ctrl+K` command shortcut and localized topbar copy. Reworked the demo voice-note flow so it now stores and renders actual recorded audio data in chat instead of only showing a placeholder label.
- In progress or partially done: The auth shell is now structurally aligned with the docs, but full bilingual coverage across the main app remains incomplete. The command palette is still a lightweight action launcher, not a full global search over vehicles, users, and requests. The voice-note path is now functional for demo mode, but it is still not the final private-storage production implementation.
- Remaining or blocked: `docs/APP_STRUCTURE.md` is stale and no longer matches the real route tree. The offline queue still exists but is not wired into active UI flows and still targets placeholder endpoints. Full Supabase-backed persistence, signed private media access, and automatic number-plate blur remain pending. Notifications are still a shortcut target, not a full notification center. Automated tests are still missing.
- Key decisions made and why: Prioritized fixes that closed spec contradictions and false affordances first: missing auth behavior, broken locale-entry behavior, incorrect permissions, missing route-level fallbacks, and fake voice-note functionality. Kept the improvements additive and low-risk so the app stayed buildable while still moving materially closer to the documented product intent.
- Known issues or tech debt introduced: Demo voice notes currently persist as data URLs for local-mode practicality, which is acceptable for the demo state model but not suitable for the final production storage design. The review also confirmed that localization coverage is still uneven across the app and will need a dedicated pass rather than more piecemeal edits.

### 2026-04-09 Night Session - App Shell Controls, Locale Fixes, and Theme Pass
- Completed: Audited the main shell and high-traffic screens against the spec and fixed the most visible gaps. Added top-level app controls for language, appearance, and notifications through `src/components/layout/topbar-locale-switcher.tsx`, `src/components/layout/appearance-switcher.tsx`, and `src/components/layout/notification-center.tsx`, then wired them into the top bar in `src/components/layout/app-topbar.tsx`. Added theme bootstrap and accent persistence in `src/lib/theme/preferences.ts` plus new shell/background theme tokens in `src/app/globals.css` and `src/app/layout.tsx`, so the app now supports light, dark, and accent-color switching without new dependencies. Extended the app layout in `src/app/[locale]/(app)/app/layout.tsx` to load shell notification data and render the improved shell background. Fixed a real admin-settings bug by updating `src/lib/demo/portal-users.ts` so `updateDemoSettings()` now accepts the nested `notifications` payload shape actually sent by the UI. Verified that end to end against the running local server by logging in as `admin-demo@example.com`, patching `/api/demo/portal`, and confirming the JSON-backed portal store changed as expected. Added or expanded translations and live language switching across the most visible user paths: dashboard, inventory list screens, vehicle cards, vehicle detail, profile, settings, chat workspace, and chat window. Updated `src/messages/en.json` and `src/messages/hi.json`, and rewired the affected route files and components so `/hi/app` now renders Hindi content instead of only changing the URL.
- In progress or partially done: Locale coverage is substantially better in the main shell and primary user screens, but not every admin page and helper surface is fully translated yet. The notification center is now a real shell control backed by portal data, but it is still a lightweight in-app activity dropdown rather than the full notification model described in the long-term spec. The quick-actions surface remains an action launcher rather than a full indexed command/search system.
- Remaining or blocked: Several lower-priority screens still contain English copy and need the same translation pass applied. The offline queue remains structurally incomplete and is still not wired to the active UI flows. Private media delivery and automatic plate blur are still pending. Full Supabase-backed persistence is still not finished for the broader portal workflows. Automated tests still do not exist for the new shell controls, locale switching, or settings persistence.
- Key decisions made and why: Put the language and appearance controls directly in the top shell because the user explicitly asked for them to be available at the top of the page, and because fixing the shell first improves every route at once. Implemented theme switching with localStorage and document-level CSS variables instead of adding a dependency, which kept the solution small and aligned with project rules. Kept profile as the place where account-level language preference is edited, while the topbar locale switch updates the active locale immediately for the current browsing session. Preserved the demo JSON-backed state model and fixed the settings payload shape rather than replacing the persistence layer mid-pass.
- Known issues or tech debt introduced: Some Windows-authored files still resist `apply_patch`, so a few rewrites had to use `Set-Content -Encoding utf8`. The translation files have grown quickly and should eventually be reorganized by domain once the remaining pages are localized. The dashboard helper still mixes translated labels with demo-domain values and would benefit from a dedicated view-model layer later.

### 2026-04-09 Late Night Session - Hindi Localization Repair
- Completed: Investigated the broken Hindi rendering reported on the live local server and traced it to two concrete root causes: a partially corrupted `src/messages/hi.json` bundle where many keys had been saved as literal question marks, and active inventory/profile surfaces that still contained hardcoded English labels. Replaced `src/messages/hi.json` with a clean UTF-8 Hindi bundle, added missing translation keys to `src/messages/en.json`, and localized the active shell sheet labels in `src/components/layout/app-topbar.tsx`. Added `src/lib/i18n/vehicle-display.ts` to centralize localized vehicle attribute labels and currency formatting, then updated `src/components/vehicle/vehicle-grid.tsx`, `src/components/vehicle/vehicle-card.tsx`, and `src/modules/inventory/components/vehicle-detail-screen.tsx` so filter labels, selected filters, fuel type, transmission, body type, and price output all respect the current locale. Updated `src/modules/profile/components/profile-form.tsx` so role badges render localized role names. Rewrote `src/components/layout/topbar-locale-switcher.tsx` with clean UTF-8 labels for the locale picker. Removed the visible English placeholder text from `public/placeholder-car.svg` so Hindi inventory cards no longer show an English fallback graphic.
- In progress or partially done: The main Hindi breakage is fixed on the active inventory and profile flows, but this was not yet expanded into a full route-by-route localization audit across every admin queue and settings sub-surface.
- Remaining or blocked: Lower-priority pages may still have isolated English copy that has not been manually exercised yet. Demo inventory record content such as make, model, city, and freeform highlights remains intentionally data-driven and may still be English until a content-localization strategy is chosen.
- Key decisions made and why: Replaced the whole Hindi bundle instead of trying to patch mojibake line by line, because Windows shell encoding had made the file-level corruption hard to reason about and the safest fix was a clean UTF-8 source of truth. Moved repeated vehicle label logic into one utility so cards, detail, and filters stop drifting across locales.
- Known issues or tech debt introduced: `src/messages/hi.json` is now clean, but the localization surface area is growing and would benefit from message-domain splitting once the rest of the app is translated. The new vehicle-display utility currently covers the values present in demo data; if more body or fuel types are introduced, its lookup table will need to be extended.

### 2026-04-09 Late Night Session - Submission Ownership and Staff Control
- Completed: Implemented the approved ownership model for seller uploads. Extended the demo submission domain with owner IDs, structured vehicle fields, asking price, media, timestamps, and listing links in `src/lib/demo/portal-types.ts`, added normalization helpers in `src/lib/demo/submission-utils.ts`, and normalized persisted portal state on read so older demo JSON records continue to work without a manual reset. Reworked `src/lib/demo/portal-workflows.ts` and `src/app/api/demo/portal/route.ts` so logged-in users can create, edit, and delete only their own pending or changes-requested submissions, while manager and admin users can edit, status-change, convert, and delete any submission. Added a real user-facing upload workspace in `src/modules/requests/components/my-submissions-panel.tsx` plus the shared editor in `src/modules/requests/components/vehicle-submission-form.tsx`, and wired it into a new `My Uploads` tab in `src/app/[locale]/(app)/app/my-requests/page.tsx`. Rebuilt the staff intake queue in `src/modules/admin/components/seller-submissions-panel.tsx` so staff can edit the intake record itself, request changes, mark reviewed, reject, delete, or convert to a listing. Added staff listing delete support and queue cleanup in `src/app/api/demo/vehicles/[listingId]/route.ts`, added a dedicated management table in `src/modules/admin/components/vehicle-management-table.tsx`, and broadened `src/modules/inventory/components/vehicle-form.tsx` so staff can set any listing status instead of only draft/published. Updated stock ID generation to use the next highest existing ID instead of array length so create/delete cycles no longer duplicate stock IDs.
- In progress or partially done: The new upload and staff controls are working in local demo mode, but they are still backed by the JSON demo store rather than Supabase tables or server actions. The shared submission editor uses lightweight inline copy rather than the full next-intl message domain, so the new flow is functional on Hindi routes but not yet fully message-bundle driven.
- Remaining or blocked: User uploads still intentionally stop being user-editable once staff converts them into inventory, matching the approved rule for this session. There is still no richer staff-side diff/history view for seller submissions, no bulk actions across listing management, and no automated Playwright coverage for the new create/edit/delete flows. Deleting a listing now cleans related request and chat records from the demo store, but the production persistence layer for that cascade does not exist yet.
- Key decisions made and why: Kept user ownership limited to submissions instead of live listings because that was the explicitly approved model and it avoids permission ambiguity once inventory becomes staff-managed. Added normalization for persisted demo state instead of forcing a reset because the demo JSON store is already being used as live session state in local development. Chose a shared submission form for both user and staff flows so the intake data shape stays consistent across the app.
- Known issues or tech debt introduced: Runtime verification added temporary demo activity entries, which were cleaned from the portal state afterward; future automated verification should ideally run against isolated demo fixtures instead of the shared local store. The submission and listing management surfaces are now materially more capable, but the demo-domain mutation logic is spread across route handlers and JSON stores that should eventually collapse into a single real backend layer.

### 2026-04-09 Session Close - Mobile-First Input And App Direction
- Completed: Captured the next-session product direction in durable project docs instead of leaving it only in chat. Added persistent rules to `AGENTS.md` covering mobile-first design, predictive inputs for large option sets, simple selectors for short fixed option sets, and future mobile-app compatibility requirements. Added a dedicated roadmap at `docs/superpowers/plans/2026-04-09-mobile-first-inputs-and-app-roadmap.md` covering smart input strategy, mobile-first UI/UX rules, critical mobile rework flows, and the recommended future Android/iOS path.
- In progress or partially done: No UI implementation was done in this closeout pass. This was documentation and planning only so the next session can start from explicit constraints.
- Remaining or blocked: The add/edit vehicle form is still too desktop-oriented and not acceptable as a 95% mobile-primary experience. Predictive make/model or other long-list inputs are still not implemented. The app also still needs a formal PWA readiness pass before any APK/IPA packaging decision can move from plan to execution.
- Key decisions made and why: Locked in a clear control heuristic for future UI work: searchable predictive inputs for long or growing lists, simple pickers for short fixed enums. Chose a phased path for mobile apps that starts with a stronger responsive web/PWA foundation and only then moves to packaging or wrapper work, because that is the fastest correct route from the current codebase.
- Known issues or tech debt introduced: None in the application itself; only roadmap and requirement docs were added or updated in this closeout step.

### 2026-04-09 Late Night Session - Auth Contrast Repair
- Completed: Investigated the washed-out text report on the auth screen and traced it to a theme mismatch between the shared document shell and the hardcoded auth background. Added `auth-shell-background` and `auth-card` theme-aware utilities in `src/app/globals.css`, switched `src/app/[locale]/(auth)/layout.tsx` to use token-based auth background styling, and updated `src/components/layout/language-switcher.tsx` so the locale pill keeps readable active and inactive states. Reworked the login, signup, and forgot-password page headers plus the shared auth forms in `src/modules/auth/components/login-form.tsx`, `src/modules/auth/components/signup-form.tsx`, and `src/modules/auth/components/forgot-password-form.tsx` to use explicit readable foreground, muted, input, and button colors instead of inheriting unstable contrast from the saved theme state.
- In progress or partially done: This pass fixed the shared auth surfaces, but other future public-facing routes should still be checked visually if they do not live inside the authenticated app shell.
- Remaining or blocked: No known blocker remains for the reported auth-text visibility bug itself. A manual visual pass on the deployed Vercel build is still recommended after the next deploy.
- Key decisions made and why: Fixed the problem at the shared auth shell and shared form level instead of patching only the login screen so signup, forgot-password, and locale controls would all stay consistent. Kept the solution dependency-free and aligned with the existing theme-token system.
- Known issues or tech debt introduced: Auth forms now intentionally override some shared primitive defaults for readability. If the auth visual design changes again later, those explicit auth classes should be reviewed together rather than piecemeal.

### 2026-04-09 Session Note - Theme Work Deferred To Next Session
- Completed: Updated the project handoff and roadmap docs so the next session starts with explicit theme-system goals instead of treating theme changes as vague UI polish. Added theme-specific project rules to `AGENTS.md`, expanded `docs/superpowers/plans/2026-04-09-mobile-first-inputs-and-app-roadmap.md` with a dedicated theme-system direction section, and rewrote `HANDOFF.md` so it reflects the real current app state and next-session priorities.
- In progress or partially done: No new runtime theme code was changed in this step. This was a planning and handoff update only.
- Remaining or blocked: The next session still needs to execute the actual shared theme pass across auth, app shell, mobile forms, bottom sheets, inventory cards, and deployed build verification.
- Key decisions made and why: Decided that future theme work should be handled as shared token and surface work, not isolated page fixes, because the auth contrast regression showed that one-off patches are too fragile.
- Known issues or tech debt introduced: `HANDOFF.md` is now useful again, but it remains an untracked local file unless it is explicitly added and committed later.

### 2026-04-09 Late Night Session - Mobile Form System, Predictive Inputs, And Verification
- Completed: Executed the deferred mobile-first and theme pass on the highest-traffic flows. Added shared token-safe surface utilities in `src/app/globals.css`, a reusable bottom-sheet searchable picker in `src/components/ui/searchable-select.tsx`, shared section and sticky-footer form shells in `src/components/forms/form-shell.tsx`, and a vehicle option catalog in `src/lib/vehicle/form-options.ts`. Rebuilt the staff vehicle editor and seller upload editor into mobile-first sectioned forms with predictive make, model, variant, and location inputs, numeric-first fields, localized copy, and sticky save actions. Updated photo upload UI to be localized and more mobile-friendly, moved the seller submissions queue to mobile-safe cards, refreshed the user upload panel copy and status handling, added predictive city selection to profile, made the inventory make filter predictive, and cleaned the app shell controls so the top bar, dropdowns, and sheets now use theme-aware surfaces instead of hardcoded white backgrounds. Tightened the chat window layout for mobile and added automated coverage for the new picker and vehicle option helpers. Fixed the Vitest setup to match the packages that are actually installed in this repo.
- In progress or partially done: The highest-traffic mobile and theme work is now in place, but a few lower-priority admin surfaces still have not had the same route-by-route localization and spacing audit. The predictive picker pattern is implemented for the current priority fields, but not yet extended to every future long-list assignment flow such as staff assignment.
- Remaining or blocked: Full Supabase-backed persistence, signed private media delivery, automatic number-plate blur, and wiring the offline queue into active flows are still pending. The PWA readiness checklist for future APK or IPA packaging still needs a dedicated pass. `docs/APP_STRUCTURE.md` remains stale. The repo still has local cleanup noise including `.server-5000.*` logs, `HANDOFF.md` as an untracked file, and the stray `nul` file.
- Key decisions made and why: Used a custom searchable sheet picker built from the existing Radix primitives instead of adding a new dependency, because the project rules favor the fastest correct path without dependency growth. Kept vehicle suggestions in a small local catalog for now so mobile predictive inputs could land immediately without blocking on backend work. Switched the staff submission queue from a table to cards because table-heavy controls were not acceptable for the documented phone-first usage pattern.
- Known issues or tech debt introduced: The predictive make, model, variant, and city suggestions are still seeded from local catalog data and should later come from shared production reference data. The Vitest setup no longer imports `jest-dom` matchers because that package is not installed in this repo, so any future tests that need those matchers should either add the dependency explicitly or stay within built-in assertions.

### 2026-04-10 Early Morning Session - Supabase Defect Remediation and Regression Coverage
- Completed: Fixed the four concrete Supabase migration defects found in review. Updated `src/lib/supabase/portal-chat.ts` so staff senders who can legitimately access a thread are auto-added to `chat_participants` before replying, which unblocks admin and manager replies. Updated `src/lib/supabase/portal-operations.ts` so staff unread counts are derived from the current viewer's participant state instead of an arbitrary participant row. Updated `src/lib/supabase/portal-reservations.ts` so duplicate active reservation requests are blocked, approving one request rejects other pending requests for the same listing, and added database-side guardrails in `supabase/migrations/20260410_001_reservation_guardrails.sql` for active reservation and waitlist uniqueness. Reworked `src/lib/supabase/portal-vehicles.ts` around explicit rollback paths so failed create and update mutations clean up newly uploaded media and restore database state instead of leaving partial inventory records behind. Added focused regression tests in `src/lib/supabase/portal-chat.test.ts`, `src/lib/supabase/portal-operations.test.ts`, `src/lib/supabase/portal-reservations.test.ts`, and `src/lib/supabase/portal-vehicles.test.ts`. Added a Vitest-only `server-only` alias in `vitest.config.ts` plus `src/test/server-only.ts` so server modules can be tested directly.
- In progress or partially done: The broader Supabase-primary migration is still a hybrid in some areas, especially seller submissions. This pass fixed the reviewed correctness defects, not the remaining feature migration backlog.
- Remaining or blocked: Supabase still needs migrations `20260408_001` through `20260408_005`, `20260409_001_portal_primary_extensions.sql`, and the new `20260410_001_reservation_guardrails.sql` applied in order. Seller submissions remain demo-backed. Plate blur, signed private media hardening beyond the current path, offline queue completion, PWA readiness planning, and repo cleanup noise are still pending.
- Key decisions made and why: Chose compensating rollback logic for vehicle mutations instead of a larger RPC or persistence redesign because it is the fastest correct way to remove the reviewed partial-write failures without widening scope. Added DB-level uniqueness for reservations and waitlist entries because app-layer checks alone are not enough against retries or races. Kept the new test coverage narrow and behavioral so it protects the reviewed defects without turning into a broad mock-heavy harness.
- Known issues or tech debt introduced: Vehicle mutation recovery is materially safer now, but it is still compensating logic across storage and database boundaries rather than a single transactional primitive. The Vitest config now contains a test-only alias for `server-only`, which is correct for this repo but should remain limited to test execution.
- Verification: `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` all passed after the fixes.

### 2026-04-10 Session Close - GitHub Push Complete and JSON Parse Error Logged
- Completed: Pushed the Supabase migration and defect-remediation work to GitHub on `origin/master` at commit `6ce8754` with message `Complete Supabase portal migration and fixes`. Left local noise files `.server-5000.err.log`, `.server-5000.out.log`, and `nul` untracked and excluded from the push.
- In progress or partially done: A new runtime client error was reported after the push: `Unexpected token '<', "<div class"... is not valid JSON`. This session did not implement the fix. The error was triaged enough to narrow the likely source area.
- Remaining or blocked: The most likely active source is the auth surface, because `src/modules/auth/components/login-form.tsx`, `src/modules/auth/components/signup-form.tsx`, and `src/modules/auth/components/forgot-password-form.tsx` still call `response.json()` directly. If `/api/auth/login`, `/api/auth/signup`, or `/api/auth/reset-password` returns an HTML error page instead of JSON, the browser will throw this exact parse error. `src/lib/demo/portal-client.ts` and the vehicle delete UI already guard failed JSON parsing, so they are less likely to be the immediate source of the raw error text. The next session needs to confirm the failing request in the browser Network tab or local server logs and then determine whether the auth API is crashing or the client-side parsing needs hardening.
- Key decisions made and why: Did not attempt a speculative fix at session close. Logged the likely root-cause area and the next verification steps instead, because the symptom indicates an HTML error response and the correct fix depends on which route is actually failing.
- Known issues or tech debt introduced: None in code during this closeout pass. The unresolved JSON parse error remains active.

### 2026-04-10 Late Evening Session - Auth JSON Error Hardening
- Completed: Resumed from the logged auth parse blocker and confirmed the failure mode with new regression tests. Added `src/app/api/auth/[...path]/route.test.ts` to prove demo signup persistence failures were escaping the auth route as thrown errors instead of JSON responses, and added `src/modules/auth/lib/read-response-json.test.ts` plus the new helper `src/modules/auth/lib/read-response-json.ts` to lock in safe response parsing for auth forms. Updated `src/app/api/auth/[...path]/route.ts` so unhandled auth-route failures now return JSON `500` responses, and updated the login, signup, and forgot-password forms to use the shared parser instead of assuming `response.json()` always succeeds.
- In progress or partially done: The previously logged auth JSON parse blocker is resolved in code and covered by targeted regressions. The broader roadmap remains unchanged: lower-priority localization audit, PWA readiness planning, seller-submission migration, offline queue wiring, plate blur, stale docs, and repo cleanup are still pending.
- Remaining or blocked: `docs/APP_STRUCTURE.md` is still stale. Seller submissions remain demo-backed. The offline queue is still not wired to active mutations. Automatic number-plate blur, the broader PWA-to-wrapper readiness pass, and local repo noise cleanup (`.server-5000.*`, `nul`, local handoff/docs churn) are still pending. This pass did not rerun the full repo `build` or full Vitest suite.
- Key decisions made and why: Fixed the error at both boundaries instead of only patching the client symptom. The auth route now guarantees JSON on unexpected failures so the API contract is stable, and the auth forms now tolerate non-JSON bodies so future regressions degrade to a normal error state instead of crashing the UI.
- Known issues or tech debt introduced: None beyond the pre-existing backlog. The auth helper is intentionally scoped to the auth surface only; other fetch clients should adopt the same pattern only when they need it.
- Verification: `npm test -- src/app/api/auth/[...path]/route.test.ts`, `npm test -- src/modules/auth/lib/read-response-json.test.ts`, `npm run typecheck`, and targeted `eslint` on the touched auth files all passed.

### 2026-04-10 Night Session - Plate Blur Pipeline And Media Variant Routing
- Completed: Implemented the pending vehicle plate-blur path in the Supabase media flow. Added `src/lib/media/plate-blur.ts` with a real raster blur step using the already-installed `sharp` runtime, added `src/lib/supabase/vehicle-media-variants.ts` to model paired original and blurred image paths, and updated `src/lib/supabase/portal-storage.ts` so new vehicle uploads now store an original image plus a blurred variant and clean up partial uploads on failure. Updated `src/lib/supabase/portal-vehicles.ts` and `src/lib/supabase/portal-catalog.ts` so listings persist paired media rows and user-facing reads choose blurred variants while staff reads choose originals. Threaded the pair metadata through `src/modules/inventory/components/vehicle-form.tsx`, the vehicle API payloads, and the portal catalog wrapper. Fixed a real pending staff-path issue by switching `src/app/[locale]/(app)/app/admin/vehicles/[listingId]/edit/page.tsx` from the demo catalog to the shared portal catalog so staff edit flows now preserve original-image access in Supabase mode. Recreated and updated `docs/APP_STRUCTURE.md` so it matches the real route tree and current storage behavior, and updated `AGENTS.md` with the new paired-media rule.
- In progress or partially done: The core Supabase vehicle-media path now supports blurred user-facing images and original staff images. Existing legacy images without a saved blurred companion are only guaranteed to pick up the new pair model when they are re-saved through the staff edit flow; there is no full storage backfill job yet.
- Remaining or blocked: Seller submissions are still demo-backed instead of fully migrated to Supabase. The offline queue is still not wired into active mutation flows. The PWA readiness checklist for future APK or IPA packaging is still pending. Some lower-priority admin helper copy still needs a final localization pass. Local cleanup noise still exists in the worktree (`.server-5000.*`, `nul`).
- Key decisions made and why: Chose a paired original-and-blurred media model in the existing private `vehicle-images` bucket instead of a fake CSS blur or an SVG wrapper because only a real processed derivative keeps original pixels out of user-facing responses. Used the `sharp` package already present in the workspace rather than adding a new dependency, which kept the implementation small and verifiable. Chose a separate media-variant helper module so the selection and persistence logic stays explicit and testable instead of being buried inside the larger vehicle and catalog files.
- Known issues or tech debt introduced: Plate blur currently uses a deterministic lower-center heuristic for the plate region rather than OCR or provider-based detection. That is materially better than the previous no-op state and produces a true blurred derivative, but a future provider-backed detector could improve precision for unusual framing.
- Verification: `npm test -- src/lib/media/plate-blur.test.ts src/lib/supabase/vehicle-media-variants.test.ts src/lib/supabase/portal-storage.test.ts src/lib/supabase/portal-vehicles.test.ts`, full `npm test`, `npm run typecheck`, targeted `eslint` on the touched files, and `npm run build` all passed.

### 2026-04-10 Late Evening Session - Auth JSON Error Hardening
- Completed: Resumed from the logged auth parse blocker and confirmed the failure mode with new regression tests. Added `src/app/api/auth/[...path]/route.test.ts` to prove demo signup persistence failures were escaping the auth route as thrown errors instead of JSON responses, and added `src/modules/auth/lib/read-response-json.test.ts` plus the new helper `src/modules/auth/lib/read-response-json.ts` to lock in safe response parsing for auth forms. Updated `src/app/api/auth/[...path]/route.ts` so unhandled auth-route failures now return JSON `500` responses, and updated the login, signup, and forgot-password forms to use the shared parser instead of assuming `response.json()` always succeeds.
- In progress or partially done: The previously logged auth JSON parse blocker is resolved in code and covered by targeted regressions. The broader roadmap remains unchanged: lower-priority localization audit, PWA readiness planning, seller-submission migration, offline queue wiring, plate blur, stale docs, and repo cleanup are still pending.
- Remaining or blocked: `docs/APP_STRUCTURE.md` is still stale. Seller submissions remain demo-backed. The offline queue is still not wired to active mutations. Automatic number-plate blur, the broader PWA-to-wrapper readiness pass, and local repo noise cleanup (`.server-5000.*`, `nul`, local handoff/docs churn) are still pending. This pass did not rerun the full repo `build` or full Vitest suite.
- Key decisions made and why: Fixed the error at both boundaries instead of only patching the client symptom. The auth route now guarantees JSON on unexpected failures so the API contract is stable, and the auth forms now tolerate non-JSON bodies so future regressions degrade to a normal error state instead of crashing the UI.
- Known issues or tech debt introduced: None beyond the pre-existing backlog. The auth helper is intentionally scoped to the auth surface only; other fetch clients should adopt the same pattern only when they need it.
- Verification: `npm test -- src/app/api/auth/[...path]/route.test.ts`, `npm test -- src/modules/auth/lib/read-response-json.test.ts`, `npm run typecheck`, and targeted `eslint` on the touched auth files all passed.

### 2026-04-09 Late Night Session - Supabase Primary Portal Mutation Pass
- Completed: Added the Supabase-primary write path for the active portal. Created a new migration at `supabase/migrations/20260409_001_portal_primary_extensions.sql` to add `preferred_locale`, `stock_id`, `app_settings`, admin and manager policy extensions, improved signup profile bootstrap, admin promotion by email, and `chat_messages.voice_duration`. Added a server-only admin client in `src/lib/supabase/admin-client.ts`, Supabase-backed portal readers in `src/lib/supabase/portal-catalog.ts`, `src/lib/supabase/portal-media.ts`, and `src/lib/supabase/portal-operations.ts`, and wrapper read entry points in `src/lib/portal/*` so the visible inventory, dashboard, requests, chat, support, and profile pages now prefer Supabase when it is configured. Added real mutation modules for inquiries, reservations and waitlist, resale, chat, users and settings, vehicle inventory, media uploads, and activity logging under `src/lib/supabase/portal-*.ts`. Added real route handlers for `/api/portal`, `/api/vehicles`, `/api/vehicles/[listingId]`, `/api/inquiries`, `/api/reservations`, `/api/resale`, and `/api/chat/messages`, and switched the client portal entry point plus vehicle create, edit, and delete flows away from `/api/demo/*` when Supabase is present. Patched media handling so existing signed Supabase image URLs render correctly while forms keep submitting raw storage paths for unchanged images. Added bootstrap logic in `src/lib/auth/viewer.ts` so an existing auth user gets a `user_profiles` row automatically and `SUPABASE_ADMIN_EMAIL` is promoted to approved admin on first read. Updated locale preference lookup in `src/lib/i18n/preferred-locale.ts` to read `user_profiles.preferred_locale` from Supabase first. Fixed a real notification bug in `src/lib/supabase/portal-operations.ts` where user-side notifications were incorrectly filtering by requester name instead of requester ID.
- In progress or partially done: Seller submissions are still persisted in the demo JSON domain, but `create_listing_from_submission` now bridges into Supabase inventory so converted uploads no longer disappear into the demo-only vehicle store. The active chat workspace now writes to real chat routes, but the older `ChatProvider` helper remains in the repo as a parallel Supabase implementation that is not the main path.
- Remaining or blocked: No database migration has been applied yet, so the Supabase project still needs migrations `20260408_001` through `20260408_005` plus `20260409_001` before the new production path can actually run. `SUPABASE_SERVICE_ROLE_KEY` must be present for the admin client, signed media URLs, profile bootstrap, and staff-wide reads. `SUPABASE_ADMIN_EMAIL` should be set to `Junaid557722@gmail.com` if that account should auto-promote on first authenticated read. Automatic plate blur is still intentionally deferred. Seller submissions are still demo-backed rather than fully migrated into Supabase tables. The repo still has local cleanup noise including `.server-5000.*` logs and the stray `nul` file.
- Key decisions made and why: Kept demo fallback at the client entry point and on the offline endpoint routes instead of ripping out demo mode, because it preserves local safety while making Supabase the primary path immediately when configured. Used signed URL resolution plus `unoptimized` image rendering for Supabase-hosted media as the fastest correct way to display private images without widening Next image config or making the bucket public. Auto-bootstrapped missing `user_profiles` rows and admin promotion in the viewer layer so an already-created Supabase auth user can start using the app without manual SQL surgery.
- Known issues or tech debt introduced: Seller submissions are now a hybrid path: intake and staff review are still demo-backed, while listing conversion writes into Supabase inventory. That bridge is correct enough for now but should be replaced by a full Supabase submission domain later. The new portal route is broad and should eventually be split further once the remaining demo-only actions are migrated. The read path still uses service-role-backed server helpers in several places, which is acceptable for server-only BFF code but should stay tightly scoped.
- Verification: `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` all passed in this workspace after the migration pass.

### 2026-04-09 Session Start - Pending Review
- Completed: Reviewed `SESSION.md`, `HANDOFF.md`, and the current git status to establish the true starting point for the next work block without reopening completed mobile-form or shell work.
- In progress or partially done: No product code changed in this pass. The session is now positioned to pick up from the remaining audit and production-track work.
- Remaining or blocked: Immediate pending work remains the lower-priority admin and localization audit, PWA readiness planning, Supabase-backed persistence, signed private media delivery, number-plate blur, offline queue wiring, and cleanup of local repo noise such as `.server-5000.*` logs and the stray `nul` file.
- Key decisions made and why: Used `HANDOFF.md` as the primary source of truth for current priorities because it is more concise and current than reopening discovery across the codebase. Kept this pass documentation-only so the next implementation step can start cleanly from the narrowed priority list.
- Known issues or tech debt introduced: None.

### 2026-04-09 Supabase Audit - Connection Confirmed, Production Track Still Pending
- Completed: Audited the repo after Supabase connection to identify what is already live versus what still runs on the JSON demo layer. Confirmed that auth uses Supabase sessions, signup creates auth users through Supabase, user-profile bootstrap SQL exists in migrations, and chat schema plus storage policies exist in Supabase migrations. Also confirmed that the visible app shell, inventory, requests, profile updates, staff queues, listing create and edit flows, and much of chat UI are still wired to `/api/demo/*` routes and `src/lib/demo/*` readers instead of Supabase-backed queries and mutations.
- In progress or partially done: Supabase is partially integrated. Auth and some low-level chat code are ready for production direction, but the main portal data model is still split between Supabase tables and local demo stores.
- Remaining or blocked: The next production work remains replacing demo route reads and writes with Supabase-backed data access, adding signed URL delivery for private media, implementing the plate-blur pipeline, wiring the offline queue to real API routes, and moving profile and settings persistence fully off the demo store.
- Key decisions made and why: Framed the gap as a data-layer migration problem instead of a connection problem because the environment and schema wiring alone do not make the app production-backed. Prioritized concrete file-level evidence over assumptions so the next implementation pass can focus on the actual remaining integration surfaces.
- Known issues or tech debt introduced: None. This was an audit-only pass.

### 2026-04-09 Late Night Session - Mobile Form System, Predictive Inputs, And Verification
- Completed: Executed the deferred mobile-first and theme pass on the highest-traffic flows. Added shared token-safe surface utilities in `src/app/globals.css`, a reusable bottom-sheet searchable picker in `src/components/ui/searchable-select.tsx`, shared section and sticky-footer form shells in `src/components/forms/form-shell.tsx`, and a vehicle option catalog in `src/lib/vehicle/form-options.ts`. Rebuilt the staff vehicle editor and seller upload editor into mobile-first sectioned forms with predictive make, model, variant, and location inputs, numeric-first fields, localized copy, and sticky save actions. Updated photo upload UI to be localized and more mobile-friendly, moved the seller submissions queue to mobile-safe cards, refreshed the user upload panel copy and status handling, added predictive city selection to profile, made the inventory make filter predictive, and cleaned the app shell controls so the top bar, dropdowns, and sheets now use theme-aware surfaces instead of hardcoded white backgrounds. Tightened the chat window layout for mobile and added automated coverage for the new picker and vehicle option helpers. Fixed the Vitest setup to match the packages that are actually installed in this repo.
- In progress or partially done: The highest-traffic mobile and theme work is now in place, but a few lower-priority admin surfaces still have not had the same route-by-route localization and spacing audit. The predictive picker pattern is implemented for the current priority fields, but not yet extended to every future long-list assignment flow such as staff assignment.
- Remaining or blocked: Full Supabase-backed persistence, signed private media delivery, automatic number-plate blur, and wiring the offline queue into active flows are still pending. The PWA readiness checklist for future APK or IPA packaging still needs a dedicated pass. `docs/APP_STRUCTURE.md` remains stale. The repo still has local cleanup noise including `.server-5000.*` logs, `HANDOFF.md` as an untracked file, and the stray `nul` file.
- Key decisions made and why: Used a custom searchable sheet picker built from the existing Radix primitives instead of adding a new dependency, because the project rules favor the fastest correct path without dependency growth. Kept vehicle suggestions in a small local catalog for now so mobile predictive inputs could land immediately without blocking on backend work. Switched the staff submission queue from a table to cards because table-heavy controls were not acceptable for the documented phone-first usage pattern.
- Known issues or tech debt introduced: The predictive make, model, variant, and city suggestions are still seeded from local catalog data and should later come from shared production reference data. The Vitest setup no longer imports `jest-dom` matchers because that package is not installed in this repo, so any future tests that need those matchers should either add the dependency explicitly or stay within built-in assertions.

## 2026-04-11

### Seller Submission Migration, Offline Replay Wiring, And Closeout Docs
- Completed: Finished the remaining code-side backlog that could be completed locally. Added `supabase/migrations/20260411_001_seller_submission_upgrade.sql`, `src/lib/supabase/portal-submission-media.ts`, `src/lib/supabase/portal-submission-records.ts`, and `src/lib/supabase/portal-submissions.ts` so seller submissions now create, edit, delete, update status, and convert to listings through the Supabase path instead of staying demo-backed in Supabase mode. Submission media now persists under submission-owned storage paths and is cloned into listing-owned uploads during conversion so submission cleanup cannot delete live listing media. Wired the offline queue into the real portal action path by updating `src/lib/offline/db.ts`, `src/lib/offline/sync.ts`, `src/lib/demo/portal-client.ts`, and the affected inventory and chat surfaces, so inquiry, reservation, resale, and chat-message actions queue offline and replay through `/api/portal` or `/api/demo/portal` when connectivity returns. Added `docs/PWA_READINESS.md`, localized the seller-submissions and my-requests route copy further, synced `docs/APP_STRUCTURE.md`, refreshed the top-level `HANDOFF.md` snapshot, and updated `AGENTS.md` with the new submission-media ownership rule.
- In progress or partially done: The main user-side offline replay path is now live, but the broader offline story is still partial. Staff moderation actions are not queued, there is not yet a queued-action history or retry surface, and packaged-shell validation has not been run. Legacy vehicle images saved before the paired-media rollout still require manual re-save or a future backfill job to guarantee blurred public derivatives.
- Remaining or blocked: The real Supabase project still needs the migration chain applied through `20260411_001_seller_submission_upgrade.sql`, plus the required production env vars must exist. Lower-priority admin localization cleanup still remains. The PWA checklist is now documented, but icons, splash assets, offline E2E coverage, and wrapper or install-path validation are still pending. Local cleanup noise still exists in the worktree: `.server-5000.*` and `nul`.
- Key decisions made and why: Kept submission media and listing media as separate ownership domains so later submission edits or deletes cannot destroy listing assets. Limited offline replay to the main user create flows because those actions are easy to retry safely and map cleanly onto the existing portal action transport. Preserved demo fallback at the portal route and client layer so local development stays usable even without live Supabase configuration.
- Known issues or tech debt introduced: Vitest commands hit a sandbox `spawn EPERM` failure in this environment and had to be rerun with elevated permissions for final verification. The new seller-submission path still depends on a real migration apply before it can work against the live Supabase project.
- Verification: `npm test -- src/lib/supabase/portal-submissions.test.ts src/lib/demo/portal-client.test.ts src/lib/offline/sync.test.ts`, full `npm test`, `npm run typecheck`, targeted `eslint` on the touched files, and `npm run build` all passed.

### Live Validation And GitHub Push Follow-up
- Completed: Confirmed that Android and iOS packaging work is intentionally deferred for now. Checked local Supabase environment availability before live validation and found that `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, and `SUPABASE_ADMIN_EMAIL` are not present in this shell or repo checkout. Pushed the completed production backlog work to `origin/master` with commit `a9d1b1a`.
- In progress or partially done: Live Supabase data validation is approved by the user but blocked in this environment until the required env vars and test-account credentials are available locally.
- Remaining or blocked: Need admin, manager, and user test credentials plus the Supabase env vars loaded into the local shell or an accessible validation environment before exercising real live flows. The GitHub CLI is not installed locally, so a PR-based GitHub workflow is unavailable; direct `git` push remains the available path.
- Key decisions made and why: Deferred all Android and iOS packaging checks because the user explicitly said packaging is not in scope yet. Kept local noise files `.server-5000.*` and `nul` excluded from the intended push.
- Known issues or tech debt introduced: None.
